package by.anymask.core.repositories;

import by.anymask.core.model.Mask;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaskRepository extends JpaRepository<Mask, Long> {
}
